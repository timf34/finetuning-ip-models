# Inference output JSONLs and merged generations — regeneratable from
# OpenWeights via the *_inference_jobs.json summary files (which ARE tracked).
# Each output dir is 15-440 MB.
*_inference_outputs/
*_inference_*_outputs/
experiments/*/outputs/
experiments/*/outputs_*/
experiments/*/inference_*_outputs/

# Python
__pycache__/
*.pyc
*.pyo

# Local secrets
.env
